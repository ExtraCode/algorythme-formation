{# Un champ SmartOF à puces : liste dès qu'il y a plusieurs lignes,
   simple paragraphe quand il n'y en a qu'une. #}
{% if items|length > 1 %}
    <ul>
        {% for item in items %}
            <li>{{ item }}</li>
        {% endfor %}
    </ul>
{% else %}
    {{ items|first }}
{% endif %}
